# Template for git configuration secrets
# Copy to git-config.nix and fill in your details

{
  userName = "Your Real Name";
  userEmail = "your.real.email@example.com";
  signingKey = ""; # Optional: your GPG key ID
  extraConfig = {
    github.user = "your-github-username";
    gitlab.user = "your-gitlab-username";
  };
}